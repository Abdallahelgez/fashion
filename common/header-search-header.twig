<div class="search-header header-theme-bg-primary">
	<div class="container-sec">
		<div class="d-flex align-items-center justify-content-between search-header-bar">
			<div class="d-flex align-items-center bd-highlight header-logo">

				{# {% if template != 'home' %}
					<a onclick="goBack()" class="ic-menu-back d-block d-lg-none">
						<span style="font-weight: bold" class="{% if session.lang.code == 'ar' %}icon-arrow-right{% else %}icon-arrow-left{% endif %} header-theme-icon-primary"></span>
					</a>
				{% endif %} #}

				<a href="javascript:void(0);" onclick="window.slidingMenu.open()" class="ic menu icon-menu-top d-block d-lg-none" tabindex="1">
					<i class="bi bi-list"></i>
				</a>

				<a href="/" style="width: auto;height: fit-content;display: block">
					{% if header_settings.header_logo %}
						{% set logoUrl = header_settings.header_logo %}
					{% else %}
						{% set logoUrl = store.logo %}
					{% endif %}

					<img class="image-logo {% if header_settings.header_logo_mobile %} d-none d-lg-block {% endif %}" src="{{ imageUrl(logoUrl,{ h: 175, q: 100 }) }}" alt="{{ store.name }}" style="width: auto; max-height: 100%">

					{% if header_settings.header_logo_mobile %}
						{% set logoUrlMobile = header_settings.header_logo_mobile %}
						<img class="image-logo d-block d-lg-none" src="{{ imageUrl(logoUrlMobile ,{ h: 175, q: 100 }) }}" alt="{{ store.name }}" style="width: auto; max-height: 100%">
					{% endif %}
				</a>
			</div>
			{# <div class="p-2 d-none d-lg-flex align-items-center bd-highlight search-input lg-search-div flex-grow-1">
			
			                {% if not ('hide_search_bar' in header_settings.header_options) %}
			                <div class="form-group" style="position: relative; margin: 0px">
			                    <span class="icon-search search-input-icon" style="z-index: 1"></span>
			                    <div class="autocomplete" style="position: relative">
			                        <input type="text"
			                               data-cat-id="{{ category.id }}"
			                               class="form-control search-input-input"
			                               placeholder="{% if header_settings.header_search_placeholder %}{{ header_settings.header_search_placeholder }}{% else %}{{ locals.search_placeholder }}{% endif %}"
			                        >
			                        <div class="autocomplete-items"></div>
			                    </div>
			                </div>
			                {% endif %}
			
			            </div> #}
			<div class="top-main-menu-wrapper">
				<nav role="navigation" class="d-flex flex-grow-1 main-nav-wrapper">

					<ul class="main-nav d-flex flex-grow-1 flex-wrap menu-header-theme-text-for-primary-bg">

						{% for menu in main_navigation_menu.items %}
							{% if menu.url %}
								<li class="top-level-link {% if menu.slug == 'all_categories' %} all-categories {% endif %}">
									<a href="{{ menu.url }}" {% if menu.items | length > 0 %} class="mega-menu" {% endif %}>
										<span>{{ menu.name }}</span>
									</a>
									{% if menu.items | length > 0 %}
										<div class="sub-menu-block">
											<div class="row">
												{% for menu_sub in menu.items %}
													<div class="col-md-4 col-lg-4 col-sm-4">
														{% if menu_sub.items | length > 0 %}
															<h5 class="sub-menu-head">
																<a style="font-size: 1.16rem; color: inherit" href="{{ menu_sub.url }}">{{ menu_sub.name }}</a>
															</h5>
															<ul class="sub-menu-lists">
																{% for menu_sub_sub in menu_sub.items %}
																	<li>
																		<a href="{{ menu_sub_sub.url }}">{{ menu_sub_sub.name }}</a>
																	</li>
																{% endfor %}
															</ul>
														{% else %}
															<h2 class="sub-menu-head">
																<a style="font-size: 1.16rem; color: inherit" href="{{ menu_sub.url }}">{{ menu_sub.name }}</a>
															</h2>
														{% endif %}
													</div>
												{% endfor %}
											</div>
										</div>
									{% endif %}
								</li>
							{% endif %}
						{% endfor %}


					</ul>
				</nav>
			</div>
			<div class="bd-highlight d-flex align-items-center justify-content-center header-cart">
				{% if not ('hide_search_bar' in header_settings.header_options) %}
					<div class="d-block sm-search-icon">
						<i class="icon-search header-theme-icon-primary"></i>
					</div>
				{% endif %}
				<div class="d-none d-lg-block cart-price">
					<a href="/cart/view" style="color: inherit">
						<h3 class="cart-header-total"></h3>
					</a>
				</div>
				<div style="position: relative">
					<a class="a-shopping-cart" href="/cart/view" style="color: inherit">
						<i class="bi bi-cart3 icon-shopping_cart_black_36dp-1-1 cart-icon"></i>
						<span class="cart-badge d-none"></span>
					</a>
					{% if store.gift_order_settings.is_gift_order_customer_motivation_enabled == '1' %}
						<div id="tooltip" class="d-none popover" role="tooltip">
							<div class="arrow"></div>
							<div class="gift-popover-body">
								<img src="{{ asset_url }}gift-popover-icon.svg" width="24" height="24"/>
								<p class="h6 my-2">
									{{ locals.cart_send_as_gift }}
								</p>
								<button class="btn round primary d-block">
									<a href="/cart/view" class="no-border d-block">
										{{ locals.continue }}
									</a>
								</button>
							</div>
						</div>
					{% endif %}
				</div>
                <div class="account-area">
			{# <button zid-visible-customer="true" type="button" onclick="window.location.href='/account-profile'" class="btn btn-dark account-btn">
				<span class="icon-account_circle_black_24dp header-theme-icon-primary" style="position: relative; top: 1px"></span>
				<span style="position: relative; bottom: 2px">{{ locals.hello }}&nbsp;
					<zid-store-customer get="name"></zid-store-customer>
					&nbsp;</span>
			</button> #}
            <a zid-visible-customer="true" href="/account-profile" class="account-btn"><i class="bi bi-person"></i></a>
			<a zid-visible-guest="true" href="/auth/login" class="account-btn"><i class="bi bi-person"></i></a>
			{# <button zid-visible-guest="true" type="button" onclick="window.location.href='/auth/login'" class="btn btn-dark account-btn">
				<span class="icon-account_circle_black_24dp header-theme-icon-primary" style="position: relative; top: 1px"></span>
				<span style="position: relative; bottom: 2px">&nbsp;{{ locals.login }}&nbsp;</span>
			</button> #}
		</div>

		{% if not ('hide_country' in header_settings.header_options) or not ('hide_language' in header_settings.header_options)%}
			<div class=" country-lang d-block" data-toggle="modal" data-target="#langCurrecyModal">

				{# {% if session.lang.code == 'en' %}
					{% set change_to_lang = 'ar' %}
					{% set change_to_lang_name = 'العربية' %}
				{% else %}
					{% set change_to_lang = 'en' %}
					{% set change_to_lang_name = 'English' %}
				{% endif %}

				{% if not ('hide_country' in header_settings.header_options) %}
					<div class="currency-button-area d-flex justify-content-center">
						<button class="c-btn currency-btn">{{ session.country.name }}
							{% if session.country.city %}
								-
								{{ session.country.city.name }}
							{% endif %}
						</button>
					</div>
				{% endif %}
				{% if not ('hide_country' in header_settings.header_options) and not ('hide_language' in header_settings.header_options)%}
					<div class="vertical-line"></div>
				{% endif %}
				{% if not ('hide_language' in header_settings.header_options) %}
					<div class="language-button-area d-flex justify-content-between">
						<button type="submit" class='c-btn language-btn d-flex justify-content-between align-items-center'>
							<span class="px-2">{{ change_to_lang_name }}</span>&nbsp;
							<span class="icon-language_white"></span>
						</button>
					</div>
				{% endif %} #}
                <i class="bi bi-globe"></i>
			</div>
		{% endif %}
			</div>
		</div>
	</div>
</div>
